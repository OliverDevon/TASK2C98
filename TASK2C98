def countWords():
    fileName = input('enter the file name: ')
    wordCount = 0
    f = open(fileName, 'r')
    for line in f:
        words = line.split()
        wordCount += len(words)
    print(wordCount)
countWords()
